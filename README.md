# 🌍 Countrydotoworld - Country Explorer

เว็บแอปพลิเคชันสำหรับสำรวจข้อมูลประเทศทั่วโลก พัฒนาด้วย **Flask Framework** พร้อมระบบสมาชิกที่จัดเก็บข้อมูลด้วย **SQLite** และการจัดการความปลอดภัยผ่าน **Environment Variables**

---

## ✨ คุณสมบัติ (Features)

- **Country Discovery**: แสดงข้อมูลประเทศพร้อมธงชาติและที่ตั้งโดยการดึงข้อมูลจาก API 
- **User Authentication**: ระบบจัดการผู้ใช้ (Login/Logout) ที่เชื่อมต่อกับฐานข้อมูล SQLite ผ่าน SQLAlchemy
- **Secure Configuration**: แยกค่าความลับของระบบไว้ในไฟล์ `.env` เพื่อความปลอดภัย

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

- **`app.py`**: หัวใจหลักของแอปพลิเคชันจัดการทั้ง Routing, Database Model และการเชื่อมต่อ API
- **`instance/`**: โฟลเดอร์สำหรับเก็บไฟล์ `database.db` (SQLite) ซึ่งเป็นส่วนที่ Flask-SQLAlchemy สร้างขึ้น
- **`templates/`**: เก็บไฟล์ HTML ทั้งหมดไว้ใน templates
- **`static/`**: เก็บไฟล์ CSS ไว้ใน `style.css`
- **`.env`**: ไฟล์เก็บค่าคอนฟิกูเรชันสำคัญ เช่น `FLASK_SECRET_KEY`
- **`.gitignore`**: กำหนดไฟล์ที่ไม่ต้องการให้นำขึ้น GitHub

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Installation & Running)
---
---
1. Create and activate a virtualenv:

```powershell
python -m venv venv
.\venv\Scripts\Activate
```

2. Install requirements:

```powershell
pip install -r requirements.txt
```

3. Run the app:

```powershell
python app.py
```
4. ตั้งค่าไฟล์สภาพแวดล้อม:
```powershell
FLASK_SECRET_KEY=dev_key_12345
DATABASE_URL=sqlite:///database.db
```

##ทดสอบ login
- **Email**: test@gmail.com
- **Password**: 1234

Then open http://127.0.0.1:5000